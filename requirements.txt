# Datagarden — Free stack only

# Core
requests>=2.28
python-dotenv>=1.0
pandas>=1.5
numpy>=1.24

# eBay scraping (free public search)
beautifulsoup4>=4.12

# Voice (free Microsoft Edge TTS)
edge-tts>=7.0

# Video (free FFmpeg)
# Install: apt install ffmpeg

# Thumbnails
Pillow>=10.0

# Companies House streaming
websocket-client>=1.0

# Optional: Apify (if you want better eBay data, $5/mo free credit)
# apify-client>=1.0

# Optional: Jev — TypeSafe System One (cheap typed decisions)
typesafe-sdk>=0.7
